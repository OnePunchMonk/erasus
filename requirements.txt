# Core dependencies (match pyproject.toml [project.dependencies])
# Install package with: pip install -e .  or  pip install -e ".[dev]"
torch>=2.0
numpy>=1.24
Pillow>=9.0
tqdm>=4.60
pyyaml>=6.0
transformers>=4.30
